 <div class="grid">
    <div class="image">    
 <%max_num%>
    <% max_num = board.board.size.to_i - 1%>
<center>

            <font color="#000"><h3> Row Coordinates</h3></font>
                <input type="number" form="fire_ship_stuff" required name="row" autofocus placeholder="Row" min="0" max="<%= max_num %>">
                 <font color="#000"><h3>Column Coordinates</h3></font>
                <input type="number" form="fire_ship_stuff" required name="col" placeholder="Column" min="0" max="<%= max_num %>">
                </br>
                </br>
<form method="post" action="fire_shots" id="fire_ship_stuff">
    <button class="button">Fire</button>
</form>
</center>
 </div>
           <div align="center" class="nav"> 
                       <h2>Player Board</h2>
           <table id="mytable">
                <tr>
                <th> </th>
                <%board.board.size.times do |v|%>
                <th> <%=v%></th>
                </tr> 
                <%board.board.each_with_index do |v, i|%>
                    <tr>
                 <%v.each_with_index do |k, o|%>
                <th><font color=#0000FF> <%=k%></font></th>
                    </tr> 

            </table>     
            </div>     

            <div class="content" align="center">
            <h2>CPU Board</h2>
            <table id="cputable">
                <tr>
                <th> </th>
                <%cpuboard.board.size.times do |v|%>
                <th> <%=v%></th>
                </tr>
           
                <%cpuboard.board.each_with_index do |v, i|%>
                    <tr>
                    <th><%=i%></th>
                    <%v.each_with_index do |k, o|%>
                <th><font color=#0000FF> <%=k%></font></th>
                    </tr>  

                
            </table>            
</div>
         <div class="footer">
     <center>   <p> <font color="#000"> App by :</font> Twfdisturbed </p></center></div>       
    

</div>
</div>